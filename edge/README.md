# CloudFlare Workers Edge Functions

Edge-deployed functions for global CDN caching and API proxying.

## Files

- `worker.js` — Main worker script (deploy via wrangler)
- `wrangler.toml` — Wrangler configuration

## Deploy

```bash
cd edge
npm install -g wrangler
wrangler deploy
```
