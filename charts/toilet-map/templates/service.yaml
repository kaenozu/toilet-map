apiVersion: v1
kind: Service
metadata:
  name: {{ include "toilet-map.fullname" . }}
  labels:
    app: {{ include "toilet-map.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    app: {{ include "toilet-map.name" . }}
